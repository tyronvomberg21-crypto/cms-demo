# Joshua.digital — editable static site demo
Hand-coded site + a client-friendly editor. Content lives in `content/site.json`; `template.html` renders it; every save rebuilds and publishes in about a minute.
Edit at **app.pagescms.org** (free; clients are invited by email, no GitHub account needed).
